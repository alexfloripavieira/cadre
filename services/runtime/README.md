# keel-runtime

Python runtime for Keel — the reliability backbone for AI engineering agents.

## Install (editable)

```bash
cd services/runtime
pip install -e ".[dev]"
```

## Test

```bash
pytest
```

## Lint and format

```bash
ruff check .
ruff format .
```
